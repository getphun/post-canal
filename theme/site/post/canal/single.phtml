<!DOCTYPE html>
<html lang="en-US">
<head>
    <?= $this->meta->head($canal); ?>
</head>
<body>
    
    <h1><?= $canal->name ?></h1>
    <time><?= $canal->created->format('M d, Y') ?></time>
    
    <article><?= $canal->about ?></article>
    
    <?php if($posts): ?>
        <ol>
            <?php foreach($posts as $post): ?>
            <li><a href="<?= $post->page ?>"><?= $post->title ?></a></li>
            <?php endforeach; ?>
        </ol>
    <?php endif; ?>
    
    <?php if($pagination): ?>
        <ul>
            <?php foreach($pagination as $label => $link): ?>
            <li><a href="<?= $link ?>"><?= $label ?></a></li>
            <?php endforeach; ?>
        </ul>
    <?php endif; ?>
    
    <?= $this->meta->foot($canal); ?>
</body>
</html>